import { CapitalizadodomSeguroPipe } from './capitalizadodom-seguro.pipe';

describe('CapitalizadodomSeguroPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizadodomSeguroPipe();
    expect(pipe).toBeTruthy();
  });
});
